{
	"$schema": "https://deno.land/x/denon/schema.json",
	"scripts": {
		"start": {
			"cmd": "deno run --allow-net --allow-read src/index.ts",
			"desc": "Run src/index.ts"
		}
	},
	"watcher": {
		"match": ["src/**/*.*"]
	}
}
